import { cancan } from '../../../helpers/cancan';
import { module, test } from 'qunit';

module('Unit | Helper | cancan');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cancan(42);
  assert.ok(result);
});
